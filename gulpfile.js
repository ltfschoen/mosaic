var gulp = require('gulp'), 
    file = require('gulp-file');

// ES6 Promises Polyfill (since some browsers not supported according to caniuse.com)
var Promise = require('es6-promise').Promise;

gulp.task('default', function() {
})